// ODI SAN MISLIA PRESELIT SVE ŠTA IMA VEZE SA FIREBASEON

/* function putNewsToDatabase(baza, vijest, count) {
    baza.ref('vijesti/' + count).set({
        naslov: vijest.title,
        link: vijest.link,
        kategorija: vijest.category,
        kontent: vijest.content,
      });
} */